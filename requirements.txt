# XSSer runtime dependencies
#
# Mandatory (core engine + HTML parsing):
beautifulsoup4>=4.12.3
pycurl>=7.45.3
#
# Optional, per feature (uncomment or `pip install xsser[<extra>]`):
#   --pdf report exporter ......... fpdf2>=2.8.1
#   -d / -l dorking engine ........ ddgs>=9.0.0
#   --Dom / --reverse-check ....... selenium>=4.20.0   (+ system: geckodriver, firefox)
#   --gtk GUI + world map ......... PyGObject>=3.42, pycairo>=1.20, pygeoip>=0.3.2, Pillow>=9.0
#                                   (+ system: gir1.2-gtk-3.0 and related GObject-Introspection typelibs)
#
# Note: xsser auto-installs the optional pip libs on demand via core/_ensure.py,
# so they are declared as setuptools 'extras_require', not as hard requirements.
